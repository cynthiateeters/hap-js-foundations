---
/**
 * Type Tester Demo
 * An interactive demo for Station 4: Types of Data
 * Covers typeof, strings vs numbers, template literals, and booleans
 */
---

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Type Tester - HAP's Demo</title>
    <style>
        :root {
            --hap-orange: hsl(32, 76%, 63%);
            --hap-orange-dark: hsl(32, 76%, 45%);
            --bg-dark: hsl(220, 13%, 18%);
            --bg-card: hsl(220, 13%, 95%);
            --text-dark: hsl(220, 13%, 20%);
            --text-light: hsl(220, 13%, 96%);
            --text-muted: hsl(220, 10%, 50%);
            --border-color: hsl(220, 13%, 80%);
            --string-color: hsl(32, 76%, 45%);
            --string-bg: hsl(32, 76%, 95%);
            --number-color: hsl(210, 70%, 50%);
            --number-bg: hsl(210, 70%, 95%);
            --boolean-color: hsl(280, 60%, 50%);
            --boolean-bg: hsl(280, 60%, 95%);
            --trap-color: hsl(0, 70%, 50%);
            --trap-bg: hsl(0, 70%, 95%);
            --template-color: hsl(160, 60%, 40%);
            --template-bg: hsl(160, 60%, 95%);
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: 'Segoe UI', system-ui, -apple-system, sans-serif;
            background-color: var(--bg-dark);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 2rem 1rem;
            color: var(--text-light);
        }

        .page-header {
            text-align: center;
            margin-bottom: 2rem;
        }

        .page-header h1 {
            font-size: 1.5rem;
            color: var(--hap-orange);
            margin-bottom: 0.5rem;
        }

        .page-header p {
            color: var(--text-muted);
            font-size: 0.9rem;
        }

        .demo-container {
            background-color: var(--bg-card);
            border-radius: 16px;
            max-width: 650px;
            width: 100%;
            overflow: hidden;
            box-shadow: 0 10px 40px hsl(220, 13%, 10%, 0.15);
        }

        .demo-header {
            background: linear-gradient(135deg, var(--hap-orange), var(--hap-orange-dark));
            color: var(--text-light);
            padding: 1rem 1.5rem;
            text-align: center;
        }

        .demo-header h2 {
            font-size: 1.1rem;
            font-weight: 600;
        }

        .section {
            padding: 1.25rem;
            border-bottom: 1px solid var(--border-color);
        }

        .section:last-of-type {
            border-bottom: none;
        }

        .section-header {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            margin-bottom: 1rem;
            padding-bottom: 0.5rem;
            border-bottom: 2px solid;
        }

        .section-header .icon {
            font-size: 1.25rem;
        }

        .section-header h3 {
            font-size: 0.9rem;
            font-weight: 600;
        }

        /* typeof Section */
        .typeof-section {
            background-color: var(--string-bg);
        }

        .typeof-section .section-header {
            border-color: var(--string-color);
        }

        .typeof-section .section-header h3 {
            color: var(--string-color);
        }

        .typeof-tester {
            display: flex;
            align-items: center;
            gap: 0.75rem;
            flex-wrap: wrap;
            margin-bottom: 1rem;
        }

        .typeof-input {
            flex: 1;
            min-width: 150px;
            padding: 0.6rem 0.8rem;
            border: 2px solid var(--border-color);
            border-radius: 6px;
            font-size: 1rem;
            font-family: 'Consolas', 'Monaco', monospace;
        }

        .typeof-input:focus {
            outline: none;
            border-color: var(--hap-orange);
        }

        .typeof-btn {
            padding: 0.6rem 1.2rem;
            border: none;
            border-radius: 6px;
            font-size: 0.9rem;
            font-weight: 600;
            cursor: pointer;
            background-color: var(--string-color);
            color: white;
            transition: transform 0.15s;
        }

        .typeof-btn:hover {
            transform: scale(1.02);
        }

        .typeof-result {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            padding: 0.75rem;
            background: white;
            border-radius: 6px;
            font-family: 'Consolas', 'Monaco', monospace;
        }

        .typeof-result .code {
            color: var(--text-muted);
        }

        .typeof-result .type {
            font-weight: bold;
            padding: 0.2rem 0.5rem;
            border-radius: 4px;
        }

        .type-string {
            background-color: var(--string-bg);
            color: var(--string-color);
        }

        .type-number {
            background-color: var(--number-bg);
            color: var(--number-color);
        }

        .type-boolean {
            background-color: var(--boolean-bg);
            color: var(--boolean-color);
        }

        .type-undefined {
            background-color: hsl(220, 13%, 90%);
            color: var(--text-muted);
        }

        .quick-types {
            display: flex;
            flex-wrap: wrap;
            gap: 0.5rem;
            margin-top: 1rem;
            padding-top: 1rem;
            border-top: 1px dashed var(--border-color);
        }

        .quick-type-btn {
            padding: 0.4rem 0.75rem;
            border: 1px solid var(--border-color);
            border-radius: 4px;
            font-family: 'Consolas', 'Monaco', monospace;
            font-size: 0.8rem;
            cursor: pointer;
            background: white;
            color: var(--text-dark);
            transition: all 0.15s;
        }

        .quick-type-btn:hover {
            background-color: var(--string-bg);
            border-color: var(--string-color);
        }

        /* Trap Section */
        .trap-section {
            background-color: var(--trap-bg);
        }

        .trap-section .section-header {
            border-color: var(--trap-color);
        }

        .trap-section .section-header h3 {
            color: var(--trap-color);
        }

        .trap-demo {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 1rem;
            margin-bottom: 1rem;
        }

        .trap-card {
            background: white;
            border-radius: 8px;
            padding: 1rem;
            text-align: center;
        }

        .trap-card h4 {
            font-size: 0.75rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            color: var(--text-muted);
            margin-bottom: 0.5rem;
        }

        .trap-expression {
            font-family: 'Consolas', 'Monaco', monospace;
            font-size: 1.1rem;
            padding: 0.5rem;
            background: hsl(220, 13%, 97%);
            border-radius: 4px;
            margin-bottom: 0.5rem;
        }

        .trap-result {
            font-family: 'Consolas', 'Monaco', monospace;
            font-size: 1.25rem;
            font-weight: bold;
        }

        .trap-result.string-result {
            color: var(--string-color);
        }

        .trap-result.number-result {
            color: var(--number-color);
        }

        .trap-explanation {
            background: white;
            border-radius: 6px;
            padding: 0.75rem;
            font-size: 0.85rem;
            color: var(--text-dark);
            border-left: 3px solid var(--trap-color);
        }

        /* Template Section */
        .template-section {
            background-color: var(--template-bg);
        }

        .template-section .section-header {
            border-color: var(--template-color);
        }

        .template-section .section-header h3 {
            color: var(--template-color);
        }

        .template-inputs {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 0.75rem;
            margin-bottom: 1rem;
        }

        .template-input-group label {
            display: block;
            font-size: 0.75rem;
            color: var(--text-muted);
            margin-bottom: 0.25rem;
        }

        .template-input-group input {
            width: 100%;
            padding: 0.5rem;
            border: 2px solid var(--border-color);
            border-radius: 6px;
            font-family: 'Consolas', 'Monaco', monospace;
            font-size: 0.9rem;
        }

        .template-input-group input:focus {
            outline: none;
            border-color: var(--template-color);
        }

        .template-comparison {
            display: grid;
            gap: 0.75rem;
        }

        .template-way {
            background: white;
            border-radius: 8px;
            padding: 0.75rem;
        }

        .template-way h4 {
            font-size: 0.7rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            color: var(--text-muted);
            margin-bottom: 0.5rem;
        }

        .template-code {
            font-family: 'Consolas', 'Monaco', monospace;
            font-size: 0.8rem;
            padding: 0.5rem;
            background: hsl(220, 13%, 97%);
            border-radius: 4px;
            margin-bottom: 0.5rem;
            white-space: pre-wrap;
            word-break: break-all;
        }

        .template-output {
            font-family: 'Consolas', 'Monaco', monospace;
            font-size: 0.9rem;
            padding: 0.5rem;
            background: hsl(160, 60%, 92%);
            border-radius: 4px;
            color: var(--template-color);
        }

        /* Boolean Section */
        .boolean-section {
            background-color: var(--boolean-bg);
        }

        .boolean-section .section-header {
            border-color: var(--boolean-color);
        }

        .boolean-section .section-header h3 {
            color: var(--boolean-color);
        }

        .boolean-comparison {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 1rem;
        }

        .boolean-card {
            background: white;
            border-radius: 8px;
            padding: 1rem;
            text-align: center;
        }

        .boolean-card h4 {
            font-size: 0.75rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            color: var(--text-muted);
            margin-bottom: 0.5rem;
        }

        .boolean-value {
            font-family: 'Consolas', 'Monaco', monospace;
            font-size: 1.25rem;
            font-weight: bold;
            padding: 0.5rem;
            background: hsl(220, 13%, 97%);
            border-radius: 4px;
            margin-bottom: 0.5rem;
        }

        .boolean-type {
            font-size: 0.8rem;
            padding: 0.3rem 0.6rem;
            border-radius: 4px;
            display: inline-block;
        }

        .boolean-type.is-boolean {
            background-color: var(--boolean-bg);
            color: var(--boolean-color);
        }

        .boolean-type.is-string {
            background-color: var(--string-bg);
            color: var(--string-color);
        }

        .boolean-warning {
            margin-top: 1rem;
            padding: 0.75rem;
            background: white;
            border-radius: 6px;
            font-size: 0.85rem;
            color: var(--text-dark);
            border-left: 3px solid var(--boolean-color);
        }

        .tip-section {
            padding: 1rem 1.5rem;
            background-color: hsl(32, 76%, 95%);
        }

        .tip-section p {
            font-size: 0.85rem;
            color: var(--text-dark);
        }

        .tip-section strong {
            color: var(--hap-orange-dark);
        }

        .tip-section code {
            background-color: hsl(220, 13%, 90%);
            padding: 0.1rem 0.3rem;
            border-radius: 3px;
            font-size: 0.8rem;
        }

        .back-link {
            margin-top: 1.5rem;
            color: var(--hap-orange);
            text-decoration: none;
            font-size: 0.9rem;
        }

        .back-link:hover {
            text-decoration: underline;
        }

        @media (max-width: 500px) {
            .trap-demo,
            .template-inputs,
            .boolean-comparison {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <!--
    HAP Educational Content © 2026 Cynthia Teeters. All rights reserved.
    HyBit A. ProtoBot (HAP) character and the apprentice learning methodology are proprietary educational innovations.
    -->

    <header class="page-header">
        <h1>Type Tester</h1>
        <p>Explore strings, numbers, booleans, and the typeof operator!</p>
    </header>

    <main class="demo-container">
        <div class="demo-header">
            <h2>🔍 What Type Is It?</h2>
        </div>

        <!-- typeof Section -->
        <section class="section typeof-section">
            <div class="section-header">
                <span class="icon">🏷️</span>
                <h3>THE typeof OPERATOR</h3>
            </div>

            <div class="typeof-tester">
                <input type="text" id="typeof-input" class="typeof-input" placeholder="Type any value..." value='"HAP"'>
                <button class="typeof-btn" onclick="checkType()">Check Type</button>
            </div>

            <div class="typeof-result" id="typeof-result">
                <span class="code">typeof</span>
                <span id="result-value">"HAP"</span>
                <span>=</span>
                <span class="type type-string" id="result-type">"string"</span>
            </div>

            <div class="quick-types">
                <span style="font-size: 0.8rem; color: var(--text-muted);">Try these:</span>
                <button class="quick-type-btn" onclick="setAndCheck('&quot;HAP&quot;')">"HAP"</button>
                <button class="quick-type-btn" onclick="setAndCheck('42')">42</button>
                <button class="quick-type-btn" onclick="setAndCheck('true')">true</button>
                <button class="quick-type-btn" onclick="setAndCheck('&quot;5&quot;')">"5"</button>
                <button class="quick-type-btn" onclick="setAndCheck('5')">5</button>
                <button class="quick-type-btn" onclick="setAndCheck('&quot;true&quot;')">"true"</button>
            </div>
        </section>

        <!-- The Type Trap -->
        <section class="section trap-section">
            <div class="section-header">
                <span class="icon">⚠️</span>
                <h3>THE TYPE TRAP: "5" + 1</h3>
            </div>

            <div class="trap-demo">
                <div class="trap-card">
                    <h4>With quotes (string)</h4>
                    <div class="trap-expression">"5" + 1</div>
                    <div class="trap-result string-result">"51"</div>
                </div>
                <div class="trap-card">
                    <h4>Without quotes (number)</h4>
                    <div class="trap-expression">5 + 1</div>
                    <div class="trap-result number-result">6</div>
                </div>
            </div>

            <div class="trap-explanation">
                <strong>Why?</strong> When you use <code>+</code> with a string, JavaScript converts the other value
                to a string too! The quotes make <code>"5"</code> a string, not a number. Use <code>typeof</code>
                to check, and <code>Number()</code> to convert.
            </div>
        </section>

        <!-- Template Literals -->
        <section class="section template-section">
            <div class="section-header">
                <span class="icon">✨</span>
                <h3>TEMPLATE LITERALS</h3>
            </div>

            <div class="template-inputs">
                <div class="template-input-group">
                    <label>robotName:</label>
                    <input type="text" id="tpl-name" value="HAP" oninput="updateTemplates()">
                </div>
                <div class="template-input-group">
                    <label>creationYear:</label>
                    <input type="number" id="tpl-year" value="2024" oninput="updateTemplates()">
                </div>
            </div>

            <div class="template-comparison">
                <div class="template-way">
                    <h4>Old way (concatenation)</h4>
                    <div class="template-code" id="old-way-code">"Hello! I am " + robotName + ", created in " + creationYear + "."</div>
                    <div class="template-output" id="old-way-output">Hello! I am HAP, created in 2024.</div>
                </div>
                <div class="template-way">
                    <h4>New way (template literal)</h4>
                    <div class="template-code" id="new-way-code">`Hello! I am ${robotName}, created in ${creationYear}.`</div>
                    <div class="template-output" id="new-way-output">Hello! I am HAP, created in 2024.</div>
                </div>
            </div>
        </section>

        <!-- Booleans -->
        <section class="section boolean-section">
            <div class="section-header">
                <span class="icon">✓✗</span>
                <h3>BOOLEANS VS "BOOLEANS"</h3>
            </div>

            <div class="boolean-comparison">
                <div class="boolean-card">
                    <h4>Real boolean</h4>
                    <div class="boolean-value">true</div>
                    <span class="boolean-type is-boolean">typeof: "boolean"</span>
                </div>
                <div class="boolean-card">
                    <h4>String that looks like boolean</h4>
                    <div class="boolean-value">"true"</div>
                    <span class="boolean-type is-string">typeof: "string"</span>
                </div>
            </div>

            <div class="boolean-warning">
                <strong>Watch out!</strong> <code>true</code> (no quotes) is a boolean.
                <code>"true"</code> (with quotes) is a string! They look similar but are completely
                different types. When in doubt, use <code>typeof</code>.
            </div>
        </section>

        <section class="tip-section">
            <p>
                <strong>🟠 HAP's Tip:</strong> Types matter! <code>"5"</code> and <code>5</code> look the same
                but behave differently. Always use <code>typeof</code> when something unexpected happens—it saved
                me from a 20-minute debugging session!
            </p>
        </section>
    </main>

    <a href="/stations/station4/" class="back-link">← Back to Station 4</a>

    <script is:inline>
        function checkType() {
            const input = document.getElementById('typeof-input').value.trim();
            const resultValue = document.getElementById('result-value');
            const resultType = document.getElementById('result-type');

            if (!input) {
                resultValue.textContent = '(empty)';
                resultType.textContent = '"undefined"';
                resultType.className = 'type type-undefined';
                return;
            }

            // Try to evaluate the input safely
            let value;
            let type;

            try {
                // Check for string literals
                if ((input.startsWith('"') && input.endsWith('"')) ||
                    (input.startsWith("'") && input.endsWith("'"))) {
                    value = input.slice(1, -1);
                    type = 'string';
                }
                // Check for boolean
                else if (input === 'true') {
                    value = true;
                    type = 'boolean';
                }
                else if (input === 'false') {
                    value = false;
                    type = 'boolean';
                }
                // Check for number
                else if (!isNaN(Number(input)) && input !== '') {
                    value = Number(input);
                    type = 'number';
                }
                // Check for undefined/null
                else if (input === 'undefined') {
                    value = undefined;
                    type = 'undefined';
                }
                else if (input === 'null') {
                    value = null;
                    type = 'object'; // typeof null is "object" in JavaScript!
                }
                else {
                    // Treat as unquoted string (variable name - show as undefined)
                    value = input;
                    type = 'undefined (not a valid value)';
                }
            } catch (e) {
                value = input;
                type = 'error';
            }

            // Display the result
            resultValue.textContent = input;

            // Format type display
            resultType.textContent = '"' + type + '"';

            // Set appropriate class
            if (type === 'string') {
                resultType.className = 'type type-string';
            } else if (type === 'number') {
                resultType.className = 'type type-number';
            } else if (type === 'boolean') {
                resultType.className = 'type type-boolean';
            } else {
                resultType.className = 'type type-undefined';
            }
        }

        function setAndCheck(value) {
            // Decode HTML entities
            const decoded = value.replace(/&quot;/g, '"');
            document.getElementById('typeof-input').value = decoded;
            checkType();
        }

        function updateTemplates() {
            const name = document.getElementById('tpl-name').value || 'HAP';
            const year = document.getElementById('tpl-year').value || '2024';

            // Update old way
            document.getElementById('old-way-code').textContent =
                '"Hello! I am " + robotName + ", created in " + creationYear + "."';
            document.getElementById('old-way-output').textContent =
                'Hello! I am ' + name + ', created in ' + year + '.';

            // Update new way
            document.getElementById('new-way-code').textContent =
                '`Hello! I am ${robotName}, created in ${creationYear}.`';
            document.getElementById('new-way-output').textContent =
                'Hello! I am ' + name + ', created in ' + year + '.';
        }

        // Allow Enter key to check type
        document.getElementById('typeof-input').addEventListener('keypress', function(e) {
            if (e.key === 'Enter') {
                checkType();
            }
        });

        // Initialize
        checkType();
        updateTemplates();
    </script>
</body>
</html>
